controller:
  replicaCount: 1

  service:
    type: ClusterIP

  nodeSelector:
    ingress-ready: "true"

  tolerations:
    - key: "node-role.kubernetes.io/control-plane"
      operator: "Exists"
      effect: "NoSchedule"

  admissionWebhooks:
    enabled: true

defaultBackend:
  enabled: true
